# ==================================
# NAZARBAN AI CHATBOT - ENVIRONMENT VARIABLES
# ==================================
# Copy this file to .env and fill in your actual values

# === REQUIRED - APPLICATION FAILS WITHOUT THESE ===

# Node Environment (production or development)
NODE_ENV=production

# JWT Secret for Admin Authentication (MUST be 32+ random characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: NO angle brackets <>, just the plain secret!
JWT_SECRET=your-64-character-random-secret-here-change-this

# Admin Password for Panel Access
ADMIN_PASSWORD=your-strong-admin-password-here

# Google Gemini API Key for AI Chat
GOOGLE_API_KEY=your-google-api-key-here

# === EMAIL CONFIGURATION ===

# Zoho Email Credentials
ZOHO_EMAIL=your-email@yourdomain.com
ZOHO_APP_PASSWORD=your-zoho-app-specific-password

# Team Email for Lead Notifications
TEAM_EMAIL=info@nazarbanai.com

# === OPTIONAL CONFIGURATION ===

# Server Port (default: 3000)
PORT=3000

# Archive blog posts older than X days (default: 7)
ARCHIVE_AFTER_DAYS=7

# === MONITORING & LOGGING (Optional) ===

# Sentry DSN for Error Monitoring (recommended for production)
# SENTRY_DSN=https://your-sentry-dsn-here

# === PRODUCTION DEPLOYMENT NOTES ===
# 1. Never commit the .env file to git (it's in .gitignore)
# 2. Set these variables in your hosting platform:
#    - Render.com: Environment Variables tab
#    - Heroku: Config Vars
#    - Railway: Variables tab
#    - Vercel: Environment Variables
# 3. JWT_SECRET must be exactly the same across all server instances
# 4. Use strong passwords (minimum 16 characters, mixed case, numbers, symbols)
